function userInfo(id: UserId) {
  if (id === 0) {
    return 'Bot';
  }
  return `User id: ${id.toUpperCase()}`;
}

// What UserId type is correct for the id argument of the userInfo() function?

// Try yourself and read the explanation: https://t.me/intspirit/867?comment=1227
