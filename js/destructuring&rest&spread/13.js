function setCoords({ x, y } = { x: 0, y: 0 }) {
  return [x, y];
}

console.log(setCoords({}));

// What will be the output?

// Try yourself and read the explanation: https://t.me/intspirit/480?comment=359
