var flag = 0;

function setFlag(isNew) {
  if (isNew) {
    var flag = 1;
  } else {
    flag = -1;
  }
}

setFlag(false);

console.log(flag);

// What will be the output?

// Try yourself and read the explanation: https://t.me/intspirit/572?comment=559
